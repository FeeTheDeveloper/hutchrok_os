{
  "name": "@hutchrok-os/connectors",
  "version": "1.0.0",
  "private": true,
  "description": "Hutchrok OS — Provider-neutral connector interfaces and adapters",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "echo \"Tests run from root\" && exit 0",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@hutchrok-os/shared": "workspace:*",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
